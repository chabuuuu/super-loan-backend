version: "3.9"

services:
  backend:
    container_name: super-loan.loan-service
    build:
      context: ./main
    restart: unless-stopped
    ports:
      - "5000:4000"
    env_file:
      - .docker.env
    networks:
      - main-network
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 200M

networks:
  main-network:
    driver: bridge
