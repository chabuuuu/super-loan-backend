version: "3.9"

services:
  backend:
    container_name: super-loan.media-service
    image: haphuthinh/loan_media-service:latest
    restart: unless-stopped
    ports:
      - "5004:4000"
    env_file:
      - .docker.env
    networks:
      - main-network
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 300M

networks:
  main-network:
    driver: bridge
